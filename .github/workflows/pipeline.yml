name: Locadora Virtual

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r locadora/requirements.txt

    - name: Lint code
      run: |
        pip install flake8
        flake8 app

    - name: Run tests
      run: |
        pip install pytest
        pytest

    - name: Run Black
      run: |
          pip install black
          black --check .

    - name: Build Docker Image
      run: |
        docker build -t locadora_virtual:1.0.0 .