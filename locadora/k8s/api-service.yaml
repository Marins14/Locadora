apiVersion: v1
kind: Service
metadata:
  name: locadora-api
  namespace: locadora

spec:
  type: NodePort

  selector:
    app: locadora-api

  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30001